---
description: ตรวจว่าผ่าน checkpoint ของ phase ปัจจุบันหรือยัง
---

Follow the steps in `.agents/workflows/checkpoint.md` exactly (it is the shared definition for both agents). Verify by running code, never from memory. Report in Thai as a table: criterion, measured value, pass/fail.
