---
description: สรุปความคืบหน้าของโปรเจกต์
---

Follow the steps in `.agents/workflows/status.md` exactly (it is the shared definition for both agents). Also include `git log --oneline -10` so I can see which agent did what recently. Do not change any files.
